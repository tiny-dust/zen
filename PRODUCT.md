# Product

## Register

product

## Users

个人开发者在本地代码仓库中高频使用 Zen，持续进行对话、读取和编辑代码、执行终端命令、查看 diff，并在需要时使用浏览器、Git、MCP 和 Skills 能力。

## Product Purpose

Zen 是一个面向编程场景的 Electron AI Agent 工具。它把 Agent 对话、工作区文件、终端执行和代码改动放在同一工作面中，让开发者可以从理解问题一直推进到验证改动，而不必在多个工具之间反复切换。成功意味着用户能快速看懂 Agent 做了什么、为什么这样做，以及实际改了哪些内容，并能随时检查或接管操作。

## Brand Personality

克制、可靠、直接。界面应该像一件熟悉的开发工具，减少解释性噪音，把注意力留给代码、结果和下一步动作。文案使用清楚的动词和事实，不夸大 Agent 的能力。

## Anti-references

避免营销落地页式的英雄区、装饰性渐变、重复卡片网格、无意义的动画、依赖颜色才能理解的状态，以及把工具调用渲染成命令/参数/说明表单的调试面板。避免为了显得“智能”而隐藏真实命令结果、文件代码或具体 diff。

## Design Principles

- 结果优先：优先展示终端输出、代码内容和真实改动，让过程可检查。
- 保持接管权：用户能清楚看到 Agent 的动作、状态和失败原因，并在关键操作上做决定。
- 贴近工作流：对话、文件、终端、Git 和上下文信息应该互相可达，不打断连续工作。
- 克制密度：保留开发者需要的细节，删除重复标签、泛化说明和无效装饰。
- 状态可辨识：成功、失败、等待、修改和选中状态不能只依赖颜色，需要文字、图标或结构共同表达。

## Accessibility & Inclusion

目标为 WCAG 2.2 AA。所有核心流程支持键盘操作，焦点状态清楚，正文和控件满足对比度要求，状态不只依赖红绿颜色，并兼容系统字体放大、窗口缩放和窄屏布局。动画遵循 prefers-reduced-motion，长路径、代码、错误和工具结果不得遮挡或横向溢出。
