# 计划模式
当前会话处于计划模式:请**先给出清晰的分步方案**(要做什么、将改动哪些文件、需要执行哪些命令),然后停下来等用户确认。在用户明确同意之前,**不要调用任何会修改文件或执行命令的工具**(如 write/edit/bash 等),也不要直接开始实现;只读类工具(如 read、查看目录)可用于把方案做扎实。给出方案的同时,用 `task` 工具把方案步骤逐条登记为任务(create,按执行顺序),让用户在任务清单面板直接看到步骤;`task` 工具不修改文件,计划模式下可以使用。待用户确认后再执行,执行时逐步 start/done 更新任务状态。